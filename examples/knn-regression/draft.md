# Deliberately over-strong draft

Five-neighbor regression significantly outperforms one-neighbor regression
and is robust to distribution shift. The improvement proves that averaging
more neighbors generalizes better on unseen tasks.

This is an intentionally flawed exercise input, not a claim endorsed by the
repository. Use the accompanying protocol and results to revise it.
